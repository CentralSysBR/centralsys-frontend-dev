--- a/src/components/ProductCard.tsx+++ b/src/components/ProductCard.tsx@@ -5,7 +5,7 @@ interface ProductCardProps {
     id: string;
     nome: string;
-    precoVenda: number;
+    precoVendaCentavos: number;
     quantidadeEstoque: number;
     imagemUrl?: string;
     quantidadeNoCarrinho?: number;
@@ -19,7 +19,7 @@ 
 export function ProductCard({
     nome,
-    precoVenda,
+    precoVendaCentavos,
     quantidadeEstoque,
     imagemUrl,
     quantidadeNoCarrinho = 0,
@@ -76,7 +76,7 @@             {/* COLUNA DIREITA — preço + estoque */}
             <div className="flex flex-col items-end justify-center gap-1">
                 <span className="font-bold text-[#2D6A4F] text-sm">
-                    {formatCurrencyBR(precoVenda)}
+                    {formatCurrencyBR(precoVendaCentavos)}
                 </span>
 
                 {mode === 'estoque' && (
--- a/src/services/vendas.ts+++ b/src/services/vendas.ts@@ -7,8 +7,8 @@ export interface VendaItem {
   id: string;
   quantidade: number;
-  precoUnitario: number;
-  precoTotal: number;
+  precoUnitarioCentavos: number;
+  precoTotalCentavos: number;
   produto: {
     nome: string;
   };
@@ -17,7 +17,7 @@ export interface Venda {
   id: string;
   criadoEm: string;
-  valorTotal: number;
+  valorTotalCentavos: number;
   metodoPagamento: string;
 
   usuario: {
--- a/src/pages/VendaDetalhe.tsx+++ b/src/pages/VendaDetalhe.tsx@@ -44,7 +44,7 @@         <p><strong>Vendedor:</strong> {venda.usuario.nome}</p>
         <p><strong>Pagamento:</strong> {venda.metodoPagamento}</p>
         <p className="font-semibold">
-          Total: Total: {formatCurrencyBR(venda.valorTotal)}
+          Total: Total: {formatCurrencyBR(venda.valorTotalCentavos)}
         </p>
       </section>
 
@@ -66,10 +66,10 @@                 <td className="p-2">{item.produto.nome}</td>
                 <td className="p-2 text-right">{item.quantidade}</td>
                 <td className="p-2 text-right">
-                  {formatCurrencyBR(item.precoUnitario)}
+                  {formatCurrencyBR(item.precoUnitarioCentavos)}
                 </td>
                 <td className="p-2 text-right font-semibold">
-                  {formatCurrencyBR(item.precoTotal)}
+                  {formatCurrencyBR(item.precoTotalCentavos)}
                 </td>
               </tr>
             ))}
--- a/src/pages/Vendas.tsx+++ b/src/pages/Vendas.tsx@@ -75,7 +75,7 @@               <td className="p-2">{venda.metodoPagamento}</td>
 
               <td className="p-2 font-semibold">
-                {formatCurrencyBR(venda.valorTotal)}
+                {formatCurrencyBR(venda.valorTotalCentavos)}
               </td>
             </tr>
           ))}
--- a/src/pages/DashboardVendas.tsx+++ b/src/pages/DashboardVendas.tsx@@ -15,7 +15,7 @@ 
 interface Venda {
   id: string;
-  valorTotal: number;
+  valorTotalCentavos: number;
   metodoPagamento: string;
   criadoEm: string;
   usuario: { nome: string };
@@ -46,7 +46,7 @@     carregarVendas();
   }, []);
 
-  const totalGeral = vendas.reduce((acc, v) => acc + Number(v.valorTotal), 0);
+  const totalGeral = vendas.reduce((acc, v) => acc + v.valorTotalCentavos, 0);
 
   return (
     <div className="min-h-screen bg-gray-50 pb-20">
@@ -93,7 +93,7 @@                   <div className="space-y-1">
                     <div className="flex items-center gap-2">
                       <span className="font-black text-lg text-gray-900">
-                        {formatCurrencyBR(venda.valorTotal)}
+                        {formatCurrencyBR(venda.valorTotalCentavos)}
                       </span>
                       <span className="text-[10px] px-2 py-0.5 rounded-full font-bold uppercase bg-gray-100 text-gray-600">
                         {venda.metodoPagamento}
--- a/src/pages/Relatorios.tsx+++ b/src/pages/Relatorios.tsx@@ -11,6 +11,7 @@ } from "../services/relatorios";
 
 import { CardLucro } from "../components/relatorios";
+import { formatCurrencyBR } from "../utils/formatCurrencyBR";
 import { CardFluxo } from "../components/relatorios/CardFluxo";
 import { GraficoFluxo } from "../components/relatorios/GraficoFluxo";
 
@@ -118,7 +119,7 @@         <ul>
           {topProdutos.map((p, i) => (
             <li key={i}>
-              {p.nome} — R$ {p.totalFaturado}
+              {p.nome} — {formatCurrencyBR(p.totalFaturadoCentavos)}
             </li>
           ))}
         </ul>
